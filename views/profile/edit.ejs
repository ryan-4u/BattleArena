<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head') %>
  <title>Edit Profile | BattleArena</title>
</head>
<body class="ba-bg">
  <%- include('../partials/navbar') %>
  <div class="container my-5">
    <%- include('../partials/flash') %>
    <div class="row justify-content-center">
      <div class="col-md-5">
        <div class="card shadow border-0 ba-card">
          <div class="card-body p-4">
            <h4 class="fw-bold mb-4"><i class="bi bi-person-gear text-warning"></i> Edit Profile</h4>
            <form action="/profile/edit?_method=PUT" method="POST">
              <div class="mb-3">
                <label class="form-label fw-semibold">In-Game Username</label>
                <input type="text" name="gameUsername" class="form-control"
                  value="<%= profileUser.gameUsername || '' %>" placeholder="Your IGN"/>
              </div>
              <div class="mb-4">
                <label class="form-label fw-semibold">Bio</label>
                <textarea name="bio" class="form-control" rows="3" maxlength="300"
                  placeholder="Tell others about yourself..."><%= profileUser.bio || '' %></textarea>
              </div>
              <div class="d-flex gap-2">
                <button class="btn btn-warning fw-bold px-4">Save</button>
                <a href="/profile" class="btn btn-outline-secondary">Cancel</a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  <%- include('../partials/scripts') %>
</body>
</html>