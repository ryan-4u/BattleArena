<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
  <a class="navbar-brand fw-bold" href="/">
    ⬡ BATTLE<span style="color:#fff; font-weight:400">ARENA</span>
  </a>
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navMenu">
    <ul class="navbar-nav ms-auto align-items-center gap-2">
      <li class="nav-item">
        <a class="nav-link" href="/tournaments">Tournaments</a>
      </li>
      <% if (currUser) { %>
        <% if (currUser.role === "organiser" || currUser.role === "admin") { %>
          <li class="nav-item">
            <a class="btn btn-warning btn-sm" href="/tournaments/new">
              <i class="bi bi-plus-lg"></i> Create Tournament
            </a>
          </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/profile">
            <i class="bi bi-person-circle"></i> <%= currUser.username %>
          </a>
        </li>
        <li class="nav-item">
          <a class="btn btn-outline-light btn-sm" href="/logout">Logout</a>
        </li>
      <% } else { %>
        <li class="nav-item">
          <a class="btn btn-outline-light btn-sm" href="/login">Login</a>
        </li>
        <li class="nav-item">
          <a class="btn btn-warning btn-sm" href="/register">Register</a>
        </li>
      <% } %>
    </ul>
  </div>
</nav>
<script src="/js/main.js"></script>